import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import postcssImport from 'postcss-import';
import postcssNormalize from 'postcss-normalize';
import discardComments from 'postcss-discard-comments';

export default defineConfig({
  plugins: [react()],
  css: {
    postcss: {
      plugins: [
        postcssImport({
          skipDuplicates: false, // Important for handling charset issues
        }),
        postcssNormalize({
          forceImport: true, // Ensures that charset declarations are normalized
        }),
        discardComments({
          removeAll: true, // This will remove all comments including @charset
        }),
      ],
    },
  },
});
